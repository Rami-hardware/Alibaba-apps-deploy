- name: restart sssd
  ansible.builtin.service:
    name: sssd
    state: restarted
- name: restart chrony
  ansible.builtin.service:
    name: "{{ 'chrony' if ansible_facts.os_family == 'Debian' else 'chronyd' }}"
    state: restarted
