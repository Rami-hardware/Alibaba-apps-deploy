- name: restart sssd
  service:
    name: sssd
    state: restarted
- name: restart chrony
  service:
    name: "{{ 'chrony' if ansible_facts.os_family == 'Debian' else 'chronyd' }}"
    state: restarted
